@using BestStoreMVC.ViewModels
@model List<CategoryViewModel>

@{
    ViewData["Title"] = "Categories List";
}

<<<<<<< HEAD
<div class="row mb-4">
    <div class="col-md-6">
        <a class="btn btn-success" asp-controller="Categores" asp-action="Create">
            <i class="fas fa-plus me-2">New Category</i>
        </a>
=======
            <div class="row mb-4">
                <div class="col-md-6">
                    <a class="btn btn-success" asp-controller="Categories" asp-action="Create">
            <i class="fas fa-plus me-2">New Category</i>
                    </a>
                </div>
                <div class="col-md-6">
                    <form asp-action="Index" method="get" class="d-flex">
                        <div class="input-group">
                            <!-- Search Icon Inside Input -->
                            <span class="input-group-text bg-transparent border-0">
                                <i class="fas fa-search text-muted"></i>
                            </span>
                            <input type="text"
                                   name="search"
                                   class="form-control border-start-0 ps-4"
                                   placeholder="Search categories..."
                                   value="@ViewData["CurrentFilter"]">
                            <!-- Icon-only Submit Button -->
                            <button type="submit" class="btn btn-outline-secondary border-start-0">
                                <i class="fas fa-search"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th scope="col" class="py-3">#</th>
                    <th scope="col" class="py-3">Name</th>
                    <th scope="col" class="py-3">Description</th>
                    <th scope="col" class="py-3">Stock</th>
                    <th scope="col" class="py-3">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>@category.Id</td>
                            <td>@category.Name</td>
                            <td>@category.Description</td>
                            <td>@category.Stock</td>
                            <td class="text-nowrap">
                                <a class="btn btn-sm btn-outline-secondary me-2"
                                   asp-controller="Categories"
                                   asp-action="Edit"
                                   asp-route-id="@category.Id">
                                    <i class="fas fa-edit me-1"></i>Edit
                                </a>
                                <a class="btn btn-sm btn-outline-danger"
                                   asp-controller="Categories"
                                   asp-action="Delete"
                                   asp-route-id="@category.Id"
                                   onclick="return confirm('Are you sure you want to delete this category?')">
                                    <i class="fas fa-trash-alt me-1"></i>Delete
                                </a>
                                <a class="btn btn-sm btn-info me-2" asp-action="Details" asp-route-id="@category.Id">
                                    <i class="fas fa-eye me-1"></i>View
                                </a>

                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center">
                            <div class="alert alert-warning mb-0" role="alert">
                                No categories found!
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
>>>>>>> 644827e (Fix controller name from "Categores" to "Categories")
    </div>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover align-middle">
        <thead class="table-dark">
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Description</th>
                <th>Stock</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in Model)
            {
                <tr>
                    <td>@category.Id</td>
                    <td>@category.Name</td>
                    <td>@category.Description</td>
                    <td>@category.Stock</td>
                    <td>
                        <a class="btn btn-sm btn-outline-secondary" asp-action="Edit" asp-route-id="@category.Id">Edit</a>
                        <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@category.Id">Delete</a>
                        <a class="btn btn-sm btn-info" asp-action="Details" asp-route-id="@category.Id">View</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>